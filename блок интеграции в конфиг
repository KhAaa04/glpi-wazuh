docker cp single-node-wazuh.manager-1:/var/ossec/etc/ossec.conf ~/ossec.conf

nano ~/ossec.conf
 
  <integration>
    <name>custom-webhook</name>
    <hook_url>http://10.200.178.85:5000/webhook</hook_url> #найти номер шлюза. тут указан через hotname -I
    <level>1</level>
  </integration>

docker cp ~/ossec.conf single-node-wazuh.manager-1:/var/ossec/etc/ossec.conf

docker restart single-node-wazuh.manager-1

docker exec -it single-node-wazuh.manager-1 cat /var/ossec/etc/ossec.conf | grep -A 10 "Webhook integration"   # проверка что блок виден
